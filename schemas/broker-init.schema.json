{
  "$id": "https://hiveme.dev/schemas/broker-init/v1.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Everything `hmc` needs to reach the cluster `hmg` is already talking to, and the\nlanguage to talk about it in.",
  "properties": {
    "language": {
      "description": "The `gui.language` of the application that copied the string, a BCP 47 tag.\nOptional: a string without it leaves an existing language alone, and a new config\ngets `en-US`.",
      "type": [
        "string",
        "null"
      ]
    },
    "password": {
      "default": "",
      "description": "Plain text, because the CONNECT packet needs it in plain text.",
      "type": "string"
    },
    "url": {
      "default": "",
      "description": "`<cluster>.s1.eu.hivemq.cloud:8883` for a HiveMQ Cloud cluster, as the console\nshows it. A URL that names no scheme is read as TLS MQTT.",
      "type": "string"
    },
    "username": {
      "default": "",
      "type": "string"
    },
    "v": {
      "default": 1,
      "description": "The format version. See [`BROKER_INIT_VERSION`].",
      "format": "uint32",
      "minimum": 1,
      "type": "integer"
    }
  },
  "title": "HiveMe broker setup string",
  "type": "object"
}
